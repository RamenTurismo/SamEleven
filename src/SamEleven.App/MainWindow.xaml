<Window
    x:Class="SamEleven.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:converters="using:CommunityToolkit.WinUI.UI.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:SamEleven.App"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:picker="using:SamEleven.App.Features.Picker"
    mc:Ignorable="d">

    <Grid d:DataContext="{d:DesignInstance local:MainWindowViewModel}">
        <Grid.Resources>
            <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
            <converters:BoolToObjectConverter
                x:Key="InverseBoolToVisibilityConverter"
                FalseValue="Visible"
                TrueValue="Collapsed" />
        </Grid.Resources>

        <NavigationView x:Name="NavigationView">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Games" Icon="Library" />
                <NavigationViewItem
                    Content="Achievements"
                    Icon="ShowResults"
                    IsEnabled="{Binding IsAnyGameSelected}" />
            </NavigationView.MenuItems>
            <Frame x:Name="ContentFrame" />
        </NavigationView>
    </Grid>
</Window>
